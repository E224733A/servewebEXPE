@model HomeIndexViewModel
@{
    ViewData["Title"] = "Administration";
}

<section class="screen-grid screen-grid-home">
    <article class="panel panel-hero">
        <p class="eyebrow">Interface Administration</p>
        <h1>Préparer les commentaires exceptionnels</h1>
        <p class="lead">
            Cette page est réservée à l'Administration. Elle permet de charger les mêmes données préparables,
            choisir une tournée, puis saisir uniquement les commentaires exceptionnels transmis au livreur.
        </p>

        <div class="hero-actions-row">
            <form asp-action="Charger" method="post" data-loading-form>
                @Html.AntiForgeryToken()
                <button type="submit" class="button button-primary">Charger les données à préparer</button>
            </form>

            <form asp-action="TesterApi" method="post" data-loading-form>
                @Html.AntiForgeryToken()
                <button type="submit" class="button button-test">Mode test API</button>
            </form>
        </div>

        <div class="status-strip status-strip-warning">
            <strong>Séparation métier</strong>
            <span>Les quantités prévues ne sont jamais modifiables depuis l'interface Administration.</span>
        </div>
        <div class="status-strip status-strip-success">
            <strong>Verrouillage</strong>
            <span>Les commentaires sont fusionnés avec les quantités Expédition au verrouillage automatique de 22h35.</span>
        </div>
    </article>

    <aside class="panel side-panel">
        <p class="eyebrow">Dernier chargement Administration</p>
        @if (Model.HasLoadedData)
        {
            <div class="metric-stack">
                <div class="metric-card">
                    <span>Date préparée</span>
                    <strong>@Model.DateTournee?.ToString("dd/MM/yyyy")</strong>
                </div>
                <div class="metric-card">
                    <span>Tournées chargées</span>
                    <strong>@Model.TourneesCount</strong>
                </div>
            </div>
            <a class="button button-secondary full-width" asp-action="Tournees">Ouvrir les tournées Administration</a>
        }
        else
        {
            <p class="muted">Aucune donnée n'a encore été chargée.</p>
        }
    </aside>
</section>

<section class="panel">
    <div class="section-header">
        <div>
            <p class="eyebrow">Verrouillage</p>
            <h2>Dernières tentatives</h2>
        </div>
        <span class="badge badge-soft">22h35</span>
    </div>

    @if (Model.RecentLocks.Count == 0)
    {
        <p class="muted">Aucune tentative de verrouillage enregistrée.</p>
    }
    else
    {
        <div class="table-wrapper">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Lot</th>
                        <th>Date tournée</th>
                        <th>Statut</th>
                        <th>Message</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.RecentLocks)
                    {
                        <tr>
                            <td>@item.IdLotVerrouillage</td>
                            <td>@item.DateTournee.ToString("dd/MM/yyyy")</td>
                            <td><span class="badge @CssStatus.ClassFor(item.Status)">@item.Status</span></td>
                            <td>@item.ApiMessage</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</section>
